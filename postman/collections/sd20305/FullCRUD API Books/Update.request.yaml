$kind: http-request
url: "{{baseUrl}}/api/books/:id"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  id: "{{defaultId}}"
body:
  type: text
  content: |-
    {
            "title": "haha" ,
            "author":"kaka",
            "price": 10
        }
auth:
  type: noauth
order: 4000
